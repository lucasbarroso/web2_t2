class Adress {
    constructor(cep, street, number, complement, is_principal) {
        this.cep = cep
        this.street = street,
        this.number = number,
        this.complement = complement,
        this.is_principal = is_principal
    }
}

export {
    Adress
}